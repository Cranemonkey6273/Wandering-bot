{
  "name": "wandering-bot-mobile",
  "version": "1.0.2",
  "private": true,
  "description": "Native iPhone and Android shell for the Wandering Bot dashboard app.",
  "type": "module",
  "scripts": {
    "doctor": "npx cap doctor",
    "prepare:offline": "node ./scripts/sync-offline-library.mjs",
    "sync": "npm run prepare:offline && npx cap sync",
    "add:android": "npx cap add android",
    "add:ios": "npx cap add ios",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "build:android:debug": "npm run sync && cd android && gradlew.bat assembleDebug",
    "build:android:release": "npm run sync && cd android && gradlew.bat bundleRelease",
    "assets": "npx capacitor-assets generate"
  },
  "dependencies": {
    "@capacitor/app": "latest",
    "@capacitor/browser": "latest",
    "@capacitor/core": "latest",
    "@capacitor/haptics": "latest",
    "@capacitor/keyboard": "latest",
    "@capacitor/splash-screen": "latest",
    "@capacitor/status-bar": "latest"
  },
  "devDependencies": {
    "@capacitor/android": "latest",
    "@capacitor/assets": "latest",
    "@capacitor/cli": "latest",
    "@capacitor/ios": "latest"
  }
}
